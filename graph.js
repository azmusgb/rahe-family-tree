import{model,matches,currentFilters,notice,stateClass,esc,relationshipRow,personById}from'./core.js';
let graph={w:1600,h:900};
const allRelationships=()=>[...(model.relationships||[]),...(model.contextRelationships||[])];
function focusConfig(){const u=new URL(location.href);return{focus:u.searchParams.get('focus')||'',scope:u.searchParams.get('scope')||'family'};}
function focusSet(focus,scope,rels){if(!focus)return null;const keep=new Set([focus]);const structural=rels.filter(r=>r.active&&['parent-child','direct-line-succession'].includes(r.type));if(scope==='ancestors'){let changed=true;while(changed){changed=false;for(const r of structural)if(keep.has(r.to)&&!keep.has(r.from)){keep.add(r.from);changed=true;}}}else if(scope==='descendants'){let changed=true;while(changed){changed=false;for(const r of structural)if(keep.has(r.from)&&!keep.has(r.to)){keep.add(r.to);changed=true;}}}else{let changed=true;while(changed){changed=false;for(const r of rels.filter(r=>r.active)){if(keep.has(r.from)&&!keep.has(r.to)){keep.add(r.to);changed=true;}if(keep.has(r.to)&&!keep.has(r.from)){keep.add(r.from);changed=true;}}}}return keep;}
function graphData(){const relAll=allRelationships();const{focus,scope}=focusConfig(),focused=focusSet(focus,scope,relAll);const people=model.people.filter(p=>matches(p)&&(!focused||focused.has(p.id)));const ids=new Set(people.map(p=>p.id));const rels=relAll.filter(r=>r.active&&ids.has(r.from)&&ids.has(r.to)&&(!currentFilters().state||r.state.toUpperCase().includes(currentFilters().state)));return{people,rels,focus,scope};}
function familyOrder(arr,rels){const byId=new Map(arr.map(p=>[p.id,p])),neighbors=new Map();for(const r of rels.filter(r=>['spouse','spouse-lead','sibling-context'].includes(r.type))){if(!neighbors.has(r.from))neighbors.set(r.from,[]);if(!neighbors.has(r.to))neighbors.set(r.to,[]);neighbors.get(r.from).push(r.to);neighbors.get(r.to).push(r.from);}const sorted=[...arr].sort((a,b)=>`${a.branch}|${a.name}`.localeCompare(`${b.branch}|${b.name}`)),out=[],seen=new Set();for(const p of sorted){if(seen.has(p.id))continue;out.push(p);seen.add(p.id);for(const id of neighbors.get(p.id)||[]){const n=byId.get(id);if(n&&!seen.has(id)){out.push(n);seen.add(id);}}}return out;}
function layout(people,rels){const rank=new Map(people.map(p=>[p.id,0])),structural=rels.filter(r=>['parent-child','direct-line-succession'].includes(r.type));for(let pass=0;pass<people.length;pass++){let changed=false;for(const r of structural){const next=Math.max(rank.get(r.to)||0,(rank.get(r.from)||0)+1);if(next!==rank.get(r.to)){rank.set(r.to,next);changed=true;}}if(!changed)break;}for(const r of rels.filter(r=>['spouse','spouse-lead'].includes(r.type))){const m=Math.max(rank.get(r.from)||0,rank.get(r.to)||0);rank.set(r.from,m);rank.set(r.to,m);}const rows=new Map();for(const p of people){const k=rank.get(p.id)||0;if(!rows.has(k))rows.set(k,[]);rows.get(k).push(p);}for(const[k,row]of rows)rows.set(k,familyOrder(row,rels));const pos=new Map(),nodeW=220,nodeH=96,gx=46,gy=96;let maxW=1200,maxRank=0;for(const[r,row]of[...rows.entries()].sort((a,b)=>a[0]-b[0])){maxRank=Math.max(maxRank,r);const width=row.length*(nodeW+gx)-gx;maxW=Math.max(maxW,width+160);const start=Math.max(80,(maxW-width)/2);row.forEach((p,i)=>pos.set(p.id,{x:start+i*(nodeW+gx),y:80+r*(nodeH+gy),w:nodeW,h:nodeH}));}return{pos,width:maxW,height:180+(maxRank+1)*(nodeH+gy)};}
export function renderTree(){const{people,rels,focus,scope}=graphData();if(!people.length)return'<div class="empty">No people match the current graph filters.</div>';const l=layout(people,rels);graph={w:l.width,h:l.height};const path=r=>{const a=l.pos.get(r.from),b=l.pos.get(r.to);if(!a||!b)return'';const x1=a.x+a.w/2,y1=a.y+a.h/2,x2=b.x+b.w/2,y2=b.y+b.h/2;if(['spouse','spouse-lead','identity-bridge','sibling-context','baptism-sponsor'].includes(r.type))return`M ${x1} ${y1} L ${x2} ${y2}`;const mid=(y1+y2)/2;return`M ${x1} ${a.y+a.h} C ${x1} ${mid}, ${x2} ${mid}, ${x2} ${b.y}`;};const edges=rels.map(r=>`<path class="edge ${esc(r.type)} state-${stateClass(r.state)}" d="${path(r)}"><title>${esc(`${r.type}: ${r.state} · ${r.role||''}`)}</title></path>`).join('');const nodes=people.map(p=>{const n=l.pos.get(p.id);return`<g class="graph-node state-${stateClass(p.state)} ${focus===p.id?'focused':''}" data-person="${p.id}" tabindex="0" role="button" aria-label="Open ${esc(p.name)} dossier" transform="translate(${n.x} ${n.y})"><rect width="${n.w}" height="${n.h}" rx="10"></rect><text x="14" y="23" class="node-branch">${esc(p.branch.slice(0,28))}</text><text x="14" y="49" class="node-name">${esc(p.name.slice(0,29))}</text><text x="14" y="71" class="node-state">${esc((p.stateTokens[0]||'QUALIFIED').slice(0,20))}</text><text x="14" y="88" class="node-id">${esc(p.id.slice(0,30))}</text></g>`;}).join('');const focusPerson=focus?personById(focus):null;return`${notice('Graph semantics','Pedigree edges remain distinct from contextual edges. Dotted sponsor, sibling-context, and derivative spouse-lead links provide research context without promoting them to proved kinship. Rejected assertions remain excluded.','control')}<section class="graph-shell"><div class="graph-toolbar"><button data-graph="fit">Fit</button><button data-graph="in" aria-label="Zoom in">+</button><button data-graph="out" aria-label="Zoom out">−</button>${focusPerson?`<span class="focus-pill">Focused: ${esc(focusPerson.name)} · ${esc(scope)}</span><button data-clear-focus>Clear focus</button>`:''}<span>${people.length} nodes · ${rels.length} visible edges</span></div><div class="legend"><span><i class="solid"></i> pedigree / supported</span><span><i class="dash"></i> provisional or unresolved</span><span><i class="identity"></i> identity bridge</span><span><i class="context"></i> contextual association</span></div><div class="graph-scroll" tabindex="0"><svg id="family-graph" viewBox="0 0 ${l.width} ${l.height}" role="img" aria-label="Rahe family research graph"><g>${edges}</g><g>${nodes}</g></svg></div></section><section><div class="section-title"><div><p class="eyebrow">ACCESSIBLE RELATIONSHIP INDEX</p><h2>Relationships and research associations</h2></div><span>${rels.length} visible</span></div><div class="relationship-table">${rels.map(relationshipRow).join('')}</div></section>`;}
export function fitGraph(){const svg=document.querySelector('#family-graph');if(svg)svg.setAttribute('viewBox',`0 0 ${graph.w} ${graph.h}`);}
export function zoomGraph(factor){const svg=document.querySelector('#family-graph');if(!svg)return;const vb=svg.viewBox.baseVal,nw=vb.width*factor,nh=vb.height*factor;svg.setAttribute('viewBox',`${vb.x+(vb.width-nw)/2} ${vb.y+(vb.height-nh)/2} ${nw} ${nh}`);}
