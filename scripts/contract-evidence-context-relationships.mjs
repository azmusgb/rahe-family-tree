import test from 'node:test';import assert from'node:assert/strict';import fs from'node:fs';const model=JSON.parse(fs.readFileSync('public/research-model.json','utf8')),audit=JSON.parse(fs.readFileSync('public/semantic-audit.json','utf8'));
test('v11.1 contextual orphan dispositions persist',()=>{assert(Number.parseFloat(model.meta.release)>=11.1);assert.equal(model.contextRelationships.length,4);for(const id of['CTX-KIN-001','CTX-KIN-002','CTX-FER-001','CTX-RAH-001'])assert(model.contextRelationships.some(r=>r.id===id));});
test('Kinsman collateral leads remain sibling-context',()=>{for(const id of['CTX-KIN-001','CTX-KIN-002']){const r=model.contextRelationships.find(x=>x.id===id);assert.equal(r.type,'sibling-context');assert.match(r.state,/PROVISIONAL/i);assert.match(r.state,/DERIVATIVE/i);}});
test('David Fleming remains sponsor only',()=>{const r=model.contextRelationships.find(x=>x.id==='CTX-FER-001');assert.equal(r.type,'baptism-sponsor');assert.match(r.role,/not a kinship assertion/i);assert.deepEqual(r.sourceIds,['C001']);});
test('Elie Davin remains derivative spouse lead',()=>{const r=model.contextRelationships.find(x=>x.id==='CTX-RAH-001');assert.equal(r.type,'spouse-lead');assert.match(r.state,/PROVISIONAL/i);assert.match(r.state,/DERIVATIVE/i);assert.doesNotMatch(r.state,/SUPPORTED/i);});
test('semantic audit keeps structural-orphan disclosure',()=>{assert(audit.checks.find(x=>x.id==='AUD-009')?.pass);assert.equal(audit.exceptions.orphanPeople.length,0);assert(audit.exceptions.structuralOrphans.length>=4);});
