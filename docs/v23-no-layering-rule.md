# No-layering rule

v23 may introduce a replacement owner only as a bounded migration step. The superseded owner must be deleted once the replacement passes its regression contract. A second permanent compatibility layer is a release blocker.

The current detail-aware Back controller therefore requires removal of the routeKey-only trail from mobile-ui-shell.js before v23 navigation work is considered complete.
